---
import SiteItem from "./site-item.astro";

type Props = {
  label: string;
  data: string[];
};

const { label, data } = Astro.props;
---

<div id={label.replace(" ", "-")} class="items-section flex flex-col gap-2">
  <p class="text-xl font-bold">{label}</p>
  {data.map((item) => <SiteItem url={item} />)}
</div>
